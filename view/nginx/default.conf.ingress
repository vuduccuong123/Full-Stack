server {
    listen       80;
    server_name  _;

    root   /usr/share/nginx/html;
    index  index.html;

    # SPA fallback, không proxy /api
    location / {
        try_files $uri $uri/ /index.html;
    }
}
